run `conda env create -f environment.yml -y` to create conda environment and proper packages.
linux/macOS is recommended.

on Windows, you should create conda environment manually and use pip instead of conda-forge channel.